# v0.8: SMP disabled, full hybrid authentication and RAM application tickets.
CONFIG_PQ_PROFILE_V08_RESUME_HYBRID=y
# Resume SHA/HMAC executes in the GATT callback; application AES-GCM stays
# on the existing v0.7 crypto worker with the existing explicit-IV wire format.
CONFIG_BT_RX_STACK_SIZE=8192
